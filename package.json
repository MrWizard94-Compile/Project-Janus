{
  "name": "project-janus",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "aether": "./packages/cli/dist/bin.js"
  },
  "description": "Aether dual-AI development platform",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @aether/cli run dev",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "aether": "pnpm --filter @aether/cli exec aether",
    "e2e:typescript": "node scripts/e2e-typescript.mjs",
    "e2e:framedblocks": "node scripts/e2e-framedblocks.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3",
    "vitest": "^3.2.1"
  }
}